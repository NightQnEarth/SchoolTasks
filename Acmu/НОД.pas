Program Den;
var
  A, B:longint;
begin

  {assign(input, 'input.txt'); reset(input);
  assign(output, 'output.txt'); rewrite(output);}
  
  read(A, B);
  
  while (a * b) <> 0 do
    if a > b then
      a:= a mod b
    else
      b:= b mod a;
      
  write(a + b)

end.